namespace com.core
{
    public class Closed
    {
        public const int TIME = 1;

        public Closed(int nType, int nId)
        {
            mType = nType;
            mId = nId;
        }

        int mType;
        int mId;
    }
}
